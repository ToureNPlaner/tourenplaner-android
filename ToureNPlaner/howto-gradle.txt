If the wrapper is not yet built or not up to date, create it:
gradle -b build-wrapper.gradle wrapper
It has to be in a separate build script because the whole problem is that we can't execute our main build script with an up to date grade. Thanks, google.

Then, use ./gradlew assembleDebug etc.
