Build with native gradle:
    gradle assembleDebug
or
    gradle assembleRelease

Build with gradle wrapper e.g. if your native gradle version somehow fails to build the project:
If the wrapper is not yet built or not up to date, create it:
    gradle wrapper
Then, use
    ./gradlew assembleDebug
or
    ./gradlew assembleRelease
